// Booking is free in Klove — there is no payment step. The session begins contacting offices
// as soon as it's created (see BookingAssistantModel.book / RequestFormView.submit). This file is
// intentionally left as a placeholder; the former Stripe PaymentSheet flow has been removed.
